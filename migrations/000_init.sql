-- =========================================================
-- 000_init.sql — Migração inicial (placeholder)
-- =========================================================
-- Esta migração serve como ponto de partida.
-- Adicione novas migrações seguindo o padrão:
--   NNN_descricao.sql  (ex: 001_create_users_table.sql)
--
-- Dicas:
--   - Use IF NOT EXISTS / IF EXISTS para idempotência
--   - Cada arquivo é executado em uma única transação
--   - A ordem de execução segue a ordenação alfabética do nome
-- =========================================================

SELECT 'Migração inicial executada com sucesso.' AS status;
